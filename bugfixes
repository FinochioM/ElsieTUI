- The TUI does not work when resizing the terminal window. FIXED.
- Right side border does not align with top border. FIXED.
- Text renders ON TOP of border. FIXED (problem was from testing with menu.zig, not actual TUI framework)
- The widgets jitter sometimess. FIXED.
- The fill does not work well wih the List widget. FIXED.
- Text Gradient vertical does not work. Diagonal looks the exact same as Horizontal. FIXED (only on lists, cannot make diagonal or vertical on a single line text)
- The colors do not match in different terminals (vscode, ghostty, etc)